checkout:
  post:
    - git submodule sync
    - git submodule update --init
    - mvn clean
    - mvn package
    - cp -R ./target/magmacube-1.0.0.jar $CIRCLE_ARTIFACTS

machine:
  java:
    version: oraclejdk8
